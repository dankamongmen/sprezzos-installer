<!-- Start of file howto/installation-howto.xml -->

<appendix id="installation-howto">
<title
>安裝 Howto</title>

<para
>本文介紹了如何在 &arch-title; (<quote
>&architecture;</quote
>)架構的電腦上，使用新版 &d-i; 來安裝 &debian; &releasename;。我們簡要說明安裝過程，但會囊括了大多數情況下您進行安裝所需的訊息。當有必要取得更多訊息時，我們會有較詳盡的說明文件 <link linkend="debian_installation_guide"
>&debian;安裝指南</link
>。 </para>

 <sect1 id="howto-preliminaries">
 <title
>前言</title>
<para
><phrase condition="unofficial-build"
> debian-installer 仍然處於 beta 測試階段。</phrase
> 如果您在安裝時碰到程式出錯，請參考 <xref linkend="submit-bug"/> 回報錯誤。倘若本文件不能夠回答您所有的疑問，那麼請將這些問題發表在 debian-boot 的通信論壇(&email-debian-boot-list;)，或者您也可以在 IRC (即 freenode 網站的 #debian-boot 頻道)上直接提問。 </para>
 </sect1>

 <sect1 id="howto-getting-images">
 <title
>啟動安裝程式</title>
<para
><phrase condition="unofficial-build"
> 在<ulink url="&url-d-i;"
> &d-i; 的首頁</ulink
>，你可看到幾個光碟映像檔的快速連結。</phrase
> Debian-cd 小組在 <ulink url="&url-debian-cd;"
>Debian CD 網頁</ulink
>中提供了使用 &d-i; 光碟映像檔的編譯版本。如果您需要關於取得這些光碟的資訊，請看<xref linkend="official-cdrom"/>。 </para
><para
>有些安裝方式需要其它種類的映像而非光碟映像檔。<phrase condition="unofficial-build"
> <ulink url="&url-d-i;"
>&d-i; 的網頁</ulink
> 上有其它映像檔的連結。</phrase
> <xref linkend="where-files"/> 會告訴您如何在 Debian 的映像網站找到映像檔。 </para
><para
>下一節我們就各種可能的安裝方法，仔細解釋如何取得所需的映像檔。 </para>

  <sect2 id="howto-getting-images-cdrom">
  <title
>光碟</title>

<para
>現在有兩種不同的 netinst 光碟映像可以用 &d-i; 安裝 &releasename;。這些映像檔是為了讓電腦透過光碟機開機，再透過網路上下載安裝額外的軟體套件，'netinst' 的名字由此得來。兩種映像檔的不同在於完整版的 netinst 映像檔包含了基本的軟件套件，而名片版映像的安裝方式則需要透過網路下載套件。如果喜歡的話，您可以下載完整映像檔，就可以不需透過網路安裝，你只需要一套光碟中的第一片就行了。 </para
><para
>您想用哪種辦法安裝，就下載相對應的映像檔，再燒成光碟。<phrase arch="i386"
> 若要利用光碟進行開機動作，得先修改 BIOS 設定，方法參照 <xref linkend="bios-setup"/>。</phrase
> <phrase arch="powerpc"
> 若要利用光碟啟動 PowerMac ，在開機時按著 <keycap
>c</keycap
> 鍵不放。其他使用光碟開機的方法請參閱 <xref linkend="boot-cd"/>。</phrase
> </para>
  </sect2>

  <sect2 condition="supports-floppy-boot" id="howto-getting-images-floppy">
  <title
>軟碟</title>
<para
>如果您無法利用光碟進行開機動作，不妨下載一些軟碟的映像檔，用它們來安裝 Debian，包括 <filename
>floppy/boot.img</filename
> 、 <filename
>floppy/root.img</filename
>，也許還需要某張驅動程式磁片。 </para
><para
>開機磁片指的是存有 <filename
>boot.img</filename
> 的那張。當使用這張磁片啟動時，它會提示您插入第二張磁片 &mdash;，這時插入存有 <filename
>root.img</filename
> 的那張磁片。 </para
><para
>倘若透過網路安裝，我們需要 <filename
>floppy/net-drivers.img</filename
>，裡面包含許多種乙太網卡的驅動程式，並支援 PCMCIA 。 </para
><para
>假如您有光碟，但無法利用它啟動，那麼您可以透過磁片啟動，再用存在驅動磁片裡的<filename
>floppy/cd-drivers.img</filename
> 接著完成透過光碟的安裝。 </para
><para
>磁片是最不可靠的儲存方式之一，因此您最好有碰到許多壞片的準備(請參看 <xref linkend="unreliable-floppies"/>)。您所下載的每個 <filename
>.img</filename
> 檔其實就是一張磁片。您可以用 dd 命令把它寫到 /dev/fd0 ，或是其他方法(細節請參看 <xref linkend="create-floppy"/> )。記得為這些磁片依序編號。 </para>
  </sect2>

  <sect2 condition="bootable-usb" id="howto-getting-images-usb">
  <title
>USB 隨身碟</title>
<para
>我們也可以透過 USB 隨身碟來安裝套件。無論到哪你都可帶著 USB 隨身碟，這是一個相當方便的 Debian 安裝工具。 </para
><para
>要將 USB 隨身碟做成安裝工具，最簡單的辦法就是下載 <filename
>hd-media/boot.img.gz</filename
>，然後利用 gunzip 把這個檔案解壓，生成 128 MB 的映像檔，再將此映像檔寫到 USB 隨身碟， USB 隨身碟的容量至少要有 128 MB。上面所說的操作會把 USB 隨身碟裡的資料全部毀掉。以後掛載這個 USB 隨身碟的話，上面就不再是 FAT 檔案系統了。接下來我們要下載一個 Debian netinst 光碟映像檔，將這個映像檔拷貝到  USB 隨身碟。檔名沒有限制，只要結尾是 <literal
>.iso</literal
> 就可以。 </para
><para
>除此之外，還有其它更為彈性的方式可以做出能啟動 debian-installer 的 USB 隨身碟，且即使是較小容量的 USB 隨身碟亦可成功。欲知詳情，請看 <xref linkend="boot-usb-files"/>。 </para
><para arch="i386"
>有些 BIOS 支援直接透過 USB 隨身碟開機，但也有的 BIOS 不支援。您需要設定 BIOS 讓電腦從 <quote
>removable drive</quote
> 或是 <quote
>USB-ZIP</quote
> 開機，使得此電腦能從 USB 隨身碟啟動。如果這樣不奏效，您可以透過磁片開機，然後再用 USB 隨身碟完成之後的安裝。若需要一些有用的提示和細節說明，請參閱 <xref linkend="usb-boot"/>。 </para
><para arch="powerpc"
>在 Macintosh 系統上使用 USB 隨身碟開機與手動使用 Open Fireware 有關。請參閱<xref linkend="usb-boot"/>。 </para>
  </sect2>

  <sect2 id="howto-getting-images-netboot">
  <title
>透過網路開機</title>
<para
>要完全從網路上啟動(即 netboot) &d-i; 也是可行的。而採取哪種方法來從網路啟動取決於您的硬體架構和網路啟動的相關設定。在 <filename
>netboot/</filename
> 目錄裡的檔案是用來從網路啟動 &d-i; 。 </para
><para arch="i386"
>最簡單的方式是使用 PXE 的網絡啟動。先解開 <filename
>netboot/pxeboot.tar.gz</filename
>，把解壓縮後的檔案放到 <filename
>/var/lib/tftpboot</filename
>，或者放到您的 tftp 伺服器的其它目錄。然後設定 DHCP 伺服器，傳送 <filename
>/pxelinux.0</filename
> 檔名給客戶端。順利的話安裝成功。欲知詳情，請參閱 <xref linkend="install-tftp"/>。 </para>
  </sect2>

  <sect2 id="howto-getting-images-hard-disk">
  <title
>從硬碟開機</title>
<para
>除了從可移動媒介啟動安裝程式，亦可從現有的安裝了其它操作系統的硬碟啟動。將 <filename
>hd-media/initrd.gz</filename
>、<filename
>hd-media/vmlinuz</filename
> 和一個 Debian 光碟映像檔下載到硬碟最上層的目錄裡。請確保這個光碟映像檔的檔案名稱是 <literal
>.iso</literal
> 結尾。接下來就是用 initrd 啟動 linux 了。 <phrase arch="i386"
> <xref linkend="boot-initrd"/> 說明如何達到這個目的。</phrase
> </para>
  </sect2>
 </sect1>

 <sect1 id="howto-installation">
<title
>安裝</title>
<para
>一旦開始安裝套件，螢幕會出現一個初始畫面。要開機啟動的話，請按 &enterkey;﹔也可以閱讀操作指南查看其它的啟動方式以及各種參數設定(請參閱 <xref linkend="boot-parms"/>)。<phrase arch="i386"
> <phrase condition="sarge"
> 如果您希望用核心版本為 2.6 的系統，在 <prompt
>boot:</prompt
> 提示符號之後鍵入 <userinput
>linux26</userinput
>。</phrase
> <phrase condition="etch"
> 如果您希望用核心版本為 2.4 的系統，在 <prompt
>boot:</prompt
> 提示符號之後鍵入 <userinput
>install24</userinput
>。</phrase
> <footnote
><para
> <phrase condition="sarge"
><footnote
><para
> 除了磁片開機，2.6 核心可以在大多數啟動方式中使用。</para
></footnote
> </phrase
> <phrase condition="etch"
> 幾乎所有啟動方式都預設使用 2.6 核心，但是以磁片開機時無法使用 2.6 核心。</phrase
> </para
></footnote
> </phrase
> </para
><para
>接下來畫面要求您選擇欲使用的語言。利用上下鍵來選擇語言，然後按 &enterkey; 以繼續下面的步驟。接下來，您要選擇您的國家，選項裡包括了那些使用您所選語言的國家。如果在列表中找不到您要找的國家，還有一個包含世界上所有國家的列表以供選擇。 </para
><para
>您還可能被要求確認一下您的鍵盤排列方式。選擇預設的那個方式，或是選擇更合適的一個。 </para
><para
>現在可以休息一下了，這時 debian-installer 會偵測您的硬體，並從光碟、磁片、USB 隨身碟或其它設備裡將其餘部分載入系統。 </para
><para
>接下來，安裝程式會嘗試偵測網路相關硬體，透過 DHCP 完成網路的設定。如果電腦沒有連接網路，或是沒有用 DHCP，那麼您也可以手動設定網路。 </para
><para
>現在該對硬碟分割磁區了。首先，您可以選擇對整個硬碟或是硬碟上的空間進行自動分割。我們建議新手或者趕時間的使用者使用自動分區。要是您不想用自動分割磁區的話，請在選單中選擇手動分割。 </para
><para arch="i386"
>如果您想保留一個既存的 DOS 或 Windows 磁區，要小心使用自動分割磁區。如果您選擇手動分割磁區，可以使用安裝程式重新設定既存 FAT 或 NTFS 分區的大小，分給 Debian 更多的安裝空間：只需選擇該分區，並指定新的大小。 </para
><para
>在接下來的畫面裡，您可以看到分割區列表，上面標出了各分區將以何種方式格式化，以及它們的掛載點在何處。選擇一個磁區再修改分區的設置或是刪除分區。如果您選擇的是自動分割，那麼只要選則 <guimenuitem
>完成分割磁區</guimenuitem
> ,使用自動分割的設定即可。請記得至少要分配一個磁區作為置換空間，並把一個磁區掛載到 <filename
>/</filename
>上。<xref linkend="partitioning"/> 有更多關於分割磁區的資訊。 </para
><para
>現在 &d-i; 會先把您的磁區格式化，然後安裝基本系統，這兩步會花些時間。接著它會安裝核心。 </para
><para
>最後一步就是安裝 boot loader 了。如果安裝程式發現您的電腦還安裝有其它作業系統，它會把它們加入開機選單並通知您。<phrase arch="i386"
>預設狀況下會把 GRUB 安裝到第一塊硬碟的開機記錄上，一般來說，這是一個不錯的方法。當然您也可以不讓它這樣做，而是把 GRUB 裝在其它地方。</phrase
> </para
><para
>&d-i; 現在會告訴您安裝已經完成。把光碟片從光碟裡取出或者移除其它啟動媒介，然後按 &enterkey; 就可以重新啟動電腦了。然後它會進入安裝過程的下一個階段，在 <xref linkend="boot-new"/> 有對它的詳盡說明。 </para
><para
>如果您想要更多的安裝過程相關資訊，請看 <xref linkend="d-i-intro"/>。 </para>
 </sect1>

 <sect1 id="howto-installation-report">
 <title
>請寄給我們一份安裝報告</title>
<para
>如果您成功地用 &d-i; 完成了安裝，請您抽空給我們寫一份報告吧。在新裝好的系統裡的有一個名為 <filename
>install-report.template</filename
> 的樣板文件，它位於 <filename
>/root</filename
> 目錄下。請填好並將它以 <classname
>installation-reports</classname
> 軟體套件的錯誤回報發送過來。至於提交錯誤回報的細節，請參照 <xref linkend="submit-bug"/>。 </para
><para
>如果您到不了 base-config 這一步，或者碰到了其它的問題，那麼您也許遇到了 debian-installler 的錯誤。請告訴我們這些問題以改進安裝程式，所以請抽空把這些錯誤報告給我們。您也可以使用 installation report 來回報錯誤。如果安裝徹底失敗了，請參閱 <xref linkend="problem-report"/>。 </para>
 </sect1>

 <sect1 id="howto-installation-finally">
 <title
>最後......</title>
<para
>我們希望您的 Debian 安裝順利愉快，並希望您能瞭解 Debian 好用之處。您或許想閱讀 <xref linkend="post-install"/>。 </para>
 </sect1>
</appendix>
<!--   End of file howto/installation-howto.xml -->
